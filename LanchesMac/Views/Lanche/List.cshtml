@model LanchesMac.ViewModels.LancheListViewModel

<div><h2>@Model.CategoriaAtual</h2></div>

<div class="row">
    @foreach (var lanche in Model.Lanches)
    {
        @*@await Html.PartialAsync("_LanchesResumo", lanche);*@
        <partial name="_LanchesResumo" for="@lanche" />
    }
</div>